Need to have tensorrt <= 7, otherwise implicit batch dimension error..

OpenCV not found: 

```bash
cd /usr/include
sudo ln -s opencv4 opencv
```

ddnynamic reconfigure:

```bash
sudo apt install ros-melodic-ddynamic-reconfigure
```